<div class="nav-container"{{#if page.component}} data-component="{{page.component.name}}" data-version="{{page.version}}"{{/if}}>
  <aside class="nav">
    <div class="panels">
{{> nav-menu}}
{{!-- We currently don't want to show the version component menu --}}
{{!-- {{> nav-explore}} --}}
    </div>
  </aside>
</div>
